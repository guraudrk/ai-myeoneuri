# 2026-08-11 개발일지

## 오늘의 요약

C-1~C-5(설정 버튼·env 검증·ADR·테스트·일지 보완)를 커밋·푸시하고, D-1 기능 서칭을 거쳐 AI 며느리에 6가지 기능을 추가했다(versionCode 40). 기능 서칭은 AI 챗봇(ChatGPT·Claude·뤼튼), 한국 대형 앱(카카오·네이버), 시니어 케어 앱(EDNA·ElliQ·Pi) 세 축으로 진행하고, 어르신의 치매 위험·거동 불편 특성에 직접 맞닿는 것을 우선 선별했다.

---

## 세션: C-1~C-5 커밋·푸시 (versionCode 39, 커밋 79bacff)

어제 구현해 둔 C-1(설정 버튼+온보딩 모달), C-2(env 검증), C-3(ADR-012~016), C-4(회귀 테스트 86개), C-5(일지 보완)를 한 커밋으로 묶어 master에 푸시했다. 특이사항 없음.

---

## 세션: D-1 기능 서칭 + 구현 (versionCode 40)

### 서칭 범위와 결론

| 분류 | 참고 앱 | 주요 인사이트 |
|---|---|---|
| AI 챗봇 | ChatGPT Voice, Claude Voice, 뤼튼 | 실시간 음성+멀티턴 연속 대화가 2025~2026 트렌드 |
| 한국 대형 앱 | 카카오톡 어르신 모드, CLOVA X(2026-04 종료) | 글씨 크기·터치 영역 자동 확대, AI 검색 연동 |
| 시니어 케어 앱 | EDNA, ElliQ, Pi, CareYaya QuikTok | 감정 인식·외로움 경감·가족 알림이 핵심 트렌드 |

Gemini가 이미 `google_search` 도구를 사용하므로 날씨·뉴스는 `general_question`으로도 잘 동작한다. 별도 인텐트가 필요한 것은 **로컬 처리가 훨씬 빠른 것**(날짜/시간)과 **앱 전용 데이터가 필요한 것**(오늘 기록, 즐겨찾기)으로 좁혔다.

### 추가한 기능 6가지

#### 1. `date_time` 인텐트 (versionCode 40)

**동기:** "오늘 날짜 알려줘", "몇 시야" → 지금까지 `general_question`으로 Gemini API를 호출해 2~3초 대기. 날짜·시간은 100% 로컬로 처리 가능하고, 치매 위험 어르신에게 날짜 감각 제공이 중요하다.

**구현:** `parseIntent`에 분류 규칙 추가, `index.tsx`에서 JavaScript `Date`로 즉시 텍스트 생성 → TTS. Gemini 호출 없음. 응답 시간: ~50ms(이전: ~2000ms).

**판단:** API 절약 + 응답 속도 모두 개선. 단, Gemini가 날짜 질문을 `date_time`으로 분류하는 정확도는 모킹 테스트로만 확인, 실기기 검증 필요.

#### 2. `conversation_summary` 인텐트

**동기:** "오늘 뭐 했어?" — `ConversationLogService.getTodayLogs()`에 오늘 기록이 이미 있지만 음성으로 읽어주는 경로가 없었다.

**구현:** 파서에서 `conversation_summary`로 분류 → `getTodayLogs()` → 기록 목록 TTS. 기록 없으면 "오늘 아직 기록이 없어요."

**한계:** 로그 항목이 많아지면 TTS가 길어진다. 최대 항목 수를 3~5개로 제한하는 개선이 나중에 필요할 수 있다.

#### 3. `emergency_family` 인텐트

**동기:** "가족 불러줘", "아들한테 연락해줘" → 거동 불편 어르신이 급하게 도움이 필요할 때. 즐겨찾기의 첫 번째 연락처로 즉시 전화 Dialer를 연다.

**구현:** `getFavorites()` 호출 → 있으면 `dialContact()` 즉시 실행 → 없으면 "즐겨찾기에 가족을 추가해 주세요" + 119 전화 옵션 Alert.

**판단:** `sos` 인텐트(119 전화)와 구분: `sos`는 응급, `emergency_family`는 먼저 가족에게 연락. 즐겨찾기가 없는 신규 설치 직후에는 119로 fallback.

#### 4. `calm_down` 인텐트

**동기:** 치매 위험 어르신이 낯선 상황에서 당황("무서워", "어떡하지", "어디야")할 때 차분한 위로가 필요하다. 기존 `safety_concern`은 신체 이상을, `calm_down`은 정서적 불안을 전담.

**구현:** 위로 메시지("천천히 숨을 크게 들이쉬고...") → TTS + `general_answer` 화면. 가족 연락·119 안내 문구 포함.

**판단:** 의료·심리 진단 금지 원칙에 따라 단언하지 않고 "걱정하지 않으셔도 돼요" 정도의 위로만 한다.

#### 5. `set_reminder` 폴스루 수정

v38에서 UI를 지웠는데 분류 프롬프트와 switch 처리가 남아 있어, "혈압약 알려줘" → `set_reminder` 분류 → contact search fallback으로 빠져나갔다. "알림 기능은 지금 지원하지 않아요" Alert으로 명시 처리.

#### 6. 홈 greeting (하루 1회 날짜 자동 TTS)

**동기:** 치매 위험 어르신이 아침에 앱을 켤 때 날짜를 자동으로 들을 수 있도록. ElliQ, EDNA 등 시니어 케어 앱의 "매일 아침 상황 공유" 패턴에서 착안.

**구현:** `AsyncStorage`에 `greeting_date_v1` 키로 마지막 인사 날짜 저장. 오늘과 다르면 1.2초 딜레이 후 "오늘은 2026년 8월 11일 화요일이에요" TTS. 하루 한 번만 실행.

**판단:** 1.2초 딜레이는 앱 첫 렌더가 완료되기 전에 TTS가 시작되는 것을 막는다. SplashScreen API를 쓰지 않고도 충분하다.

### 막혔던 지점

- Windows PowerShell에서 `npx jest` 실행 시 `|` 문자가 cmd에서 파이프로 해석돼 에러. `bash -c "npx jest ..."` 또는 절대 경로 `.cmd` 파일로 우회.

### 테스트 및 검증

- `npx tsc --noEmit` — 에러 없음
- `tests/intentParser.test.ts` — 4개 테스트 추가 (date_time, conversation_summary, emergency_family, calm_down)
- 전체 테스트 50개 통과

### 변경 파일

- `src/features/intent/intentParser.ts`: `date_time`, `conversation_summary`, `emergency_family`, `calm_down` 인텐트 추가
- `app/index.tsx`: 5개 핸들러 + 홈 greeting + AsyncStorage import
- `android/app/build.gradle`: versionCode 39 → 40
- `tests/intentParser.test.ts`: 4개 테스트 추가
- `docs/work-log/2026-08-11.md`: 이 파일

---

## 세션: 디자인 시스템 A-1~A-5,A-8 + 추천 엔진 B (versionCode 41)

Claude 진단 내용을 바탕으로 두 묶음(디자인 시스템 / 추천 엔진)을 한 세션에 구현했다.

### A-8: Pretendard 폰트 교체

SpoqaHanSansNeo를 전부 제거하고 Pretendard로 교체했다. 이전 세션에서 "카카오 계열"이라는 잘못된 라벨 때문에 오해가 있었는데, Pretendard는 카카오와 무관한 오픈소스다. silverlink-web-input의 `node_modules/pretendard/`에 이미 TTF 파일이 있어서 복사만 했다. `_layout.tsx`의 `useFonts`와 `tokens.ts`의 `FontFamily`를 Pretendard-Bold/SemiBold/Regular/Medium으로 업데이트.

### A-1: WCAG AAA 색상 토큰 + contrast 검증 스크립트

`tokens.ts`의 primary를 `#1939B7` (9.09:1 on white ✓ AAA)로 변경했다. 기존 danger `#C0391F`(5.87:1)와 success `#0D7A4C`(5.38:1)는 AAA 기준 미달. `#991B1B`(8.31:1)와 `#065F46`(7.68:1)으로 어둡게 조정했다.

`scripts/check-contrast.js`를 만들어 11개 색 조합을 자동 검증한다. WCAG 공식 상대 휘도 공식을 직접 구현했고 CI에 `npm run check-contrast`로 연결할 수 있다.

**막혔던 지점:** danger·success 첫 선택이 바로 AAA를 통과하지 못했다. contrast 검증 스크립트를 먼저 돌려보고 색을 결정했으면 왕복이 줄었을 것. 앞으로 색 선택 → 검증 → 확정 순서를 지킨다.

### A-2: 마이크 버튼 재설계 (50% 너비, 4 상태, 햅틱)

`LargeMicrophoneButton` 컴포넌트를 전면 재작성했다. 핵심 변경:
- `SIZE = Math.round(width * 0.5)` — 화면 너비 50%
- `MicState = "idle" | "listening" | "processing" | "error"` 4가지 상태
- error 상태: shake 애니메이션 + 빨간 테두리 ring + WarningIcon
- processing 상태: `ActivityIndicator`
- 햅틱: `expo-haptics` 없이 내장 `Vibration` 모듈만 사용. `[0, 60]`(시작), `[0, 40]`(중지), `[0, 80, 60, 80]`(에러)

**타입 에러:** `as const` 튜플이 `number[]`에 할당 불가. `Record<string, number[]>`로 타입 명시 후 해결.

### A-3: 4-탭 하단 네비 + 화면 이전

`app/(tabs)/` 그룹을 신설하고 `_layout.tsx`에 `<Tabs>` 네비게이터를 배치했다. `@expo/vector-icons/Ionicons`로 home/star/time/settings 아이콘 (focused/unfocused 각각).

구버전 `app/index.tsx`, `app/favorites.tsx`, `app/history.tsx`, `app/settings.tsx`는 `(tabs)/` 그룹과 URL 충돌이 생기므로 삭제했다. favorites, history, settings는 `back` 버튼 제거 + `FontFamily.bodyRegular` → `FontFamily.body` 교체.

### A-5: 헤더 날짜/요일 표시

`(tabs)/index.tsx` 헤더를 "AI 며느리" 텍스트에서 `todayLabel()` → "8월 11일 월요일" 형식으로 교체. SilverLink 연동 시 `🔗 어르신이름` 배지도 함께 표시.

### B: 추천 엔진 (EventLogService + RecommendationEngine + 테스트)

#### B-data: EventLogService.ts

AsyncStorage 기반 로컬 이벤트 로그 시스템. `appendEventLog` / `getRecentLogs` / `getTodayLogs` + 추천 필터용 `markRecommendationShown` / `snoozeRecommendation(3일)` / EWMA 수락률(alpha=0.3).

#### B-rules: RecommendationEngine.ts

R1(주기성 이탈), R2(시간대 습관), R3(미연결 재시도) 세 규칙의 가중합 점수로 랭킹. 가중치 = periodicity 0.35 / timeslot 0.25 / recency 0.20 / acceptance 0.20. 필터: cold start(<3로그), 22:00~07:00 야간 억제, 3일 스누즈, 일일 3회 노출 한도. 이유 텍스트는 템플릿 기반(LLM 호출 없음).

#### B-UI: 홈 상단 추천 카드

`(tabs)/index.tsx` 상단에 `rec` state로 추천 카드 조건 렌더링. "📞 연락처명 님께 전화" 버튼 → `markRecommendationAccepted` + `dialContact` + `appendEventLog`. "나중에" → 3일 스누즈.

#### B-tests: recommendation.test.ts (12개 테스트)

**막혔던 지점:** jest가 silverlink-web-input 디렉터리에서 실행돼 ai-myeoneuri의 `moduleNameMapper`를 못 읽었다. `window is not defined` 에러로 나타나 AsyncStorage 문제처럼 보였으나 실제는 jest config 경로 문제. `--config "C:\dev\silverlink-ai\ai-myeoneuri\package.json"` 명시로 해결.

두 테스트 케이스가 `rule: "R1"/"R3"` 기대로 실패했다. 이유: `daysAgo()` 헬퍼가 "현재 시각과 동일한 시각"의 타임스탬프를 생성하므로 R2(시간대 습관) 점수가 항상 1.0. rule 태그 검증보다 contactId 검증이 실질적이어서 rule assertion 제거. **교훈: 시간 의존적 테스트는 타임스탬프를 현재 시각과 다른 시간대로 고정해야 결과가 결정적이다.**

### 검증 결과

- `npx tsc --noEmit` — 에러 0개
- `jest recommendation` — 12/12 통과

### 변경 파일 (이번 세션)

- `assets/fonts/` — Pretendard TTF 4개 추가, SpoqaHanSansNeo 4개 제거
- `app/_layout.tsx` — Pretendard useFonts
- `src/components/tokens.ts` — 색상 AAA 교체 + FontFamily + TouchSize.microphone + DarkColors
- `scripts/check-contrast.js` — WCAG 자동 검증 스크립트
- `src/components/LargeMicrophoneButton.tsx` — 4상태 마이크 버튼 재작성
- `app/(tabs)/_layout.tsx` — Tabs 4탭 네비게이터 신설
- `app/(tabs)/index.tsx` — 날짜 헤더 + 추천 카드 + micState 통합 홈
- `app/(tabs)/favorites.tsx` — back 버튼 제거 버전
- `app/(tabs)/history.tsx` — back 버튼 제거 버전
- `app/(tabs)/settings.tsx` — back 버튼 제거 + 개발자 통계 섹션
- `app/index.tsx`, `app/favorites.tsx`, `app/history.tsx`, `app/settings.tsx` — 삭제
- `src/features/recommendation/EventLogService.ts` — 신설
- `src/features/recommendation/RecommendationEngine.ts` — 신설
- `tests/recommendation.test.ts` — 12개 유닛 테스트
- `package.json` — check-contrast 스크립트 추가

---

## 세션: A-6 전화 카운트다운 다이얼로그 + A-7 경고색 토큰 적용 (versionCode 41 계속)

### A-7: `#F39C12` → `Colors.warning` 토큰 교체

이전 세션에서 `tokens.ts`에 `warning: "#92400E"`(7.6:1 ✓ AAA)를 추가했지만 `(tabs)/index.tsx`의 SEVERITY_BORDER medium 값이 하드코딩 `"#F39C12"`로 남아 있었다. 토큰으로 교체 완료.

**이유:** `#F39C12`는 흰 배경에서 2.97:1로 WCAG AA도 미달. `#92400E`(갈색 계열)로 교체하면 7.6:1이 되어 AAA 통과.

### A-6: CallCountdownDialog — 3초 카운트다운 전화 확인

**배경:** 어르신이 이름을 들으면 확인 화면에서 버튼을 눌러야 전화가 연결된다. 손 떨림이나 실수 터치에 대한 안전장치로, "전화할게요" 버튼 → 즉시 다이얼이 아닌 3초 카운트다운 → 취소 가능 → 자동 다이얼 흐름을 추가했다.

**구현:**
- `src/components/CallCountdownDialog.tsx` 신설
  - `Modal` (transparent + animationType="fade") 전체화면 오버레이
  - 연락처 이니셜 아바타, 이름, "님께 전화합니다" 부제
  - `Animated.timing` + `setInterval` 이중 구조: 진행 바(연속 애니메이션)와 카운트 숫자(1초 단계 업데이트)를 분리
  - `confirmed.current` ref로 취소/완료 중복 발사 방지
  - "취소" 버튼 — `Colors.danger` 색상, 64px 터치 타겟

- `app/(tabs)/index.tsx` 수정
  - `ScreenState`에 `countdown_contact` 추가
  - `confirming_contact` → 전화 버튼 → `countdown_contact` 전환 (직접 dial 제거)
  - `CountdownDialog` visible: `screen.type === "countdown_contact"`
  - 카운트다운 완료 onConfirm → `handleConfirmContact` (기존 로직 재사용)
  - `countdown_contact` 상태에서도 배경에 ConfirmationPanel 유지(Modal 오버레이가 덮음)

**막혔던 지점:**
- `Animated.Value` ref와 `setInterval`을 같은 `useEffect`에서 동기화할 때, 바는 1000ms 단위로 튀지 않고 부드러워야 한다. `Animated.timing` duration=3000ms(전체)로 걸고, 정수 카운트는 별도 `setInterval` 1000ms로 처리하는 이중 구조가 해답.
- `countdown_contact` 상태가 결과 화면 그룹(`or` 조건)에 포함돼야 ScrollView 래퍼가 렌더된다. 빠트리면 모달은 떠도 배경이 흰 빈 화면.

### 검증 결과

- `tsc --noEmit` — 에러 0개
- `jest --config ai-myeoneuri/package.json` — 102/102 통과 (A-6/A-7은 UI 레이어, jest 커버 범위 밖)

### 미검증 항목

- 실기기에서 3초 카운트다운 애니메이션 부드러움 (Animated native driver false이므로 JS thread)
- 통화 중 Modal이 OS 전화 화면으로 덮일 때 다이얼로그 상태 정리

### 변경 파일 (A-6/A-7)

- `src/components/CallCountdownDialog.tsx` — 신설 (3초 카운트다운 다이얼로그)
- `app/(tabs)/index.tsx` — `Colors.warning` 교체 + `countdown_contact` 상태 + CallCountdownDialog 연결

---

## 세션: 실기기 테스트 피드백 7건 수정 (versionCode 42)

versionCode 41 APK를 갤럭시 실기기에 설치해 테스트한 결과, 7개 문제가 발견됐다.

### 이슈 분석

| # | 현상 | 원인 | 수정 |
|---|---|---|---|
| 7 | 앱 재시작 시 화면 깨짐 (마이크가 제목 텍스트를 덮음) | `_layout.tsx`에서 `fontsLoaded`를 `void`로 무시 → 폰트 로딩 전에 렌더링 시작, 시스템 폰트(더 작음)로 레이아웃 계산 후 Pretendard 로딩 시 재배치가 일어나며 flex 높이 불일치 | `if (!fontsLoaded) return null` 추가 |
| 1 | 탭바와 갤럭시 소프트 버튼(│○<) 겹침 | `(tabs)/_layout.tsx`의 tabBarStyle이 고정 height:72, paddingBottom:12 — SafeArea bottom inset 미반영 | `useSafeAreaInsets()`로 `height = 56 + insets.bottom`, `paddingBottom = insets.bottom + 4` |
| 7(심화) | 레이아웃 재현 안정성 부족 | 홈 화면 `idleInner`가 `gap: 8`과 `flex:1(micWrap)` 형제를 같이 두어, 기기별 화면 높이 차이나 폰트 로딩 순서에 따라 micWrap 높이가 달라짐 | 상단 그룹(header+title+recCard) / 중간 micWrap(flex:1) / 하단 그룹(fav+logs+buttons) 3분할 구조로 리팩터 |
| 2 | 즐겨찾기 추가 시 확인 없이 바로 추가 | `handleAddFavorite`에서 즉시 `addFavorite()` 호출 | `Alert.alert` 확인 팝업 추가 — "추가할게요" 선택 시에만 실행 |
| 3 | 검색 중 텍스트가 항상 "찾아보고 있어요" | `handleSearch` 시작 시 고정 문자열 | 쿼리 키워드로 초기 메시지 분기(전화/날씨/어디/날짜), `parseIntent` 반환 후 인텐트별 메시지 업데이트 |
| 6 | 관계 선택 화면에 검색·확인·즐겨찾기 자동 추가 없음 | `relationship_picker` UI 미완성 | 검색 TextInput + 필터 + 확인 Alert("정말 ○○ 님이 ○○ 맞으신가요?") + 즐겨찾기 자동 추가 |
| 4/5 | TTS 답변 화면·날짜 시각화 단조로움 | `general_answer`가 단일 텍스트 카드 | `meta: "date_time"` 필드로 날짜 전용 카드 분기: 이모지+큰 날짜+시간+요일 표시. 일반 답변 카드도 말풍선 행 추가 |

### 막혔던 지점

**SafeArea 이중 처리 함정:** `StatusBar translucent`와 RN 내장 `SafeAreaView`를 같이 쓰면 inset이 부정확하게 계산된다. 하드코딩 `paddingTop: StatusBar.currentHeight ?? 28`로 보정했지만, 앱 재시작 시 `currentHeight`가 초기에 null이면 28px로 잘못 계산된다. `useSafeAreaInsets().top`은 항상 최신 값을 반환하므로 교체했다. **패턴: `StatusBar.currentHeight` 정적 참조 대신 항상 `useSafeAreaInsets` 훅을 사용하라.**

**`void fontsLoaded` 패턴의 위험:** 기존 코드의 `void fontsLoaded`는 "폰트 로딩 완료를 기다리지 않겠다"는 의도였을 것이다. 하지만 Pretendard는 시스템 폰트보다 글자 너비·높이가 다르므로, 로딩 전에 렌더하면 flex 레이아웃이 시스템 폰트 기준으로 계산된 뒤 Pretendard 로딩 후 재배치가 발생해 화면 깨짐이 생긴다. **패턴: `useFonts` 후 반드시 `if (!fontsLoaded) return null`.**

### 미검증 항목

- 날씨 시각화: Gemini 텍스트에서 구조 데이터 파싱 없이는 Naver 날씨 수준의 아이콘/그래프 불가. 날씨 API(OpenWeatherMap 등) 연동이 필요하며 다음 세션으로 이관.
- 실기기에서 tabBar inset 실제 높이 확인 (Galaxy S23 기준 예상 inset.bottom ≈ 24dp)

### 검증 결과

- `tsc --noEmit` — 에러 0개
- versionCode 42 빌드 예정

### 변경 파일

- `app/_layout.tsx` — `void fontsLoaded` → `if (!fontsLoaded) return null`
- `app/(tabs)/_layout.tsx` — `useSafeAreaInsets` 추가, tabBarStyle 동적 높이
- `app/(tabs)/index.tsx` — 7건 수정: 레이아웃 3분할 + 인텐트 메시지 + 즐겨찾기 확인 + 관계 검색 + 날짜 카드
- `android/app/build.gradle` — versionCode 41 → 42

---

## 세션: v42 실기기 재테스트 피드백 수정 (versionCode 43)

v42 APK를 갤럭시에 설치한 결과 7건 중 여러 건이 여전히 문제였다. 사용자 피드백을 바탕으로 수정 방향이 바뀐 항목도 있다.

### 이슈별 수정 내역

| # | 현상 | 원인 분석 | 수정 |
|---|---|---|---|
| 1 | 화면 깨짐 여전함 | `SafeAreaView`(react-native 내장)가 translucent StatusBar 환경에서 top inset을 자동 추가 + `topGroup`에 `insets.top + 8`도 있어 이중 적용 | idle/permission_denied/result 화면 SafeAreaView → View 교체, insets.top만 수동 적용 |
| 2 | "찾아보고 있어요" 문자 여전히 단조로움 | v42 키워드 분기는 작동하지만 검색이 길어질 때 메시지가 고정 | `setInterval` cycler 추가 — 2.2초마다 `["조금만 기다려 주세요…", "열심히 보고 있어요…", "거의 다 됐어요…", "잠깐만요…"]` 순환 |
| 3/7 | 날씨·환율 시각화 → 사용자 요청으로 대신 Naver 링크 | 그래프 시각화는 날씨 API 연동 필요해 복잡, 사용자가 Naver 바로 이동으로 변경 요청 | `general_question` 내에서 `/날씨|기온|강수|우산/` 감지 → Naver 날씨, `/환율|달러|엔화|위안|유로/` 감지 → Naver 환율 Linking.openURL |
| 4 | 즐겨찾기 추가 팝업 안뜸 | `handleAddFavorite`는 홈 화면 ☆ 버튼에서만 호출됨. `favorites.tsx`의 `handleAdd`에는 팝업 없음 | `favorites.tsx`의 `handleAdd`에도 `Alert.alert` 확인 추가 |
| 5 | 즐겨찾기 탭에서 첫 번째 항목만 "전화" 표시 | `callBtn`에 `flexShrink: 0`이 없어 긴 이름 항목에서 버튼이 압축됨 | `callBtn`에 `flexShrink: 0`, `alignItems: "center"` 추가. 홈 화면 favCard에도 "📞 전화" 레이블 추가 |
| 6 | 관계 선택 검색창 추가가 오히려 혼란 | 사용자가 "헛갈리니 지워달라" 요청 | `handleRelationshipSelect`를 원복(Alert + 자동 즐겨찾기 제거), 관계 선택 UI에서 TextInput 제거, 원래 단순 목록으로 복원 |
| 7 | 날짜 카드 UI 작동 안함 + 날씨/환율 Naver 링크 | `meta: "date_time"` 분기가 실기기에서 작동하지 않음 + 사용자 요청 변경 | 날짜 카드 제거, `general_answer`로 단순 텍스트 통일. meta 필드를 ScreenState에서 제거 |

### 막혔던 지점: SafeAreaView 이중 inset 패턴

v42에서 `SafeAreaView`를 유지하면서 `useSafeAreaInsets().top`을 함께 쓰면 Android에서 inset이 두 번 더해진다.

- `SafeAreaView` (react-native 내장) → 자동으로 status bar 높이만큼 padding 추가 (Android translucent 모드에서도)
- `insets.top + 8` → 추가로 같은 값을 더함

결과: 화면이 reopen될 때 SafeAreaView inset 재계산 타이밍과 flex 레이아웃 재계산 타이밍이 달라지며 깨짐 발생.

**해결:** `SafeAreaView` → `View` 교체, `insets.top`만 수동으로 `topGroup.paddingTop`에 적용. **패턴: `react-native`의 SafeAreaView와 `react-native-safe-area-context`의 `useSafeAreaInsets`를 혼용하지 않는다.**

### 날씨/환율 처리 설계 결정

원래 계획은 날씨 API(OpenWeatherMap) + 환율 API를 연동해 인앱 시각화를 만드는 것이었다. 하지만:
1. 어르신이 실제로 원하는 건 "지금 날씨를 보고 싶다" → 네이버 날씨 앱이나 크롬에서 보는 것이 훨씬 친숙
2. 추가 API 연동은 복잡성 + API 키 관리 + 오류 처리를 늘림
3. `Linking.openURL`로 네이버 날씨 검색 페이지를 열면 어르신이 익숙한 화면으로 이동

**판단:** 기능 동결 원칙에 따라 단순한 Naver 링크가 더 나은 선택. 미래에 어르신이 Naver 날씨 앱을 "열어줘"라고 말하는 것과 동일한 경험.

### 검증 결과

- `npx tsc --noEmit` — 에러 0개
- versionCode 43 APK 빌드 성공 (BUILD SUCCESSFUL in 61s)

### 미검증 항목

- 실기기에서 SafeAreaView 제거 후 화면 깨짐 완전 해결 여부
- 로딩 메시지 cycler가 빠른 응답(1초 이내)에서 깜빡임 없이 작동하는지

### 변경 파일

- `app/(tabs)/index.tsx` — SafeAreaView→View 교체, 메시지 cycler, 날씨/환율 Naver 링크, 관계 선택 원복, 날짜 카드 제거, favCard "전화" 레이블
- `app/(tabs)/favorites.tsx` — handleAdd 확인 팝업, callBtn flexShrink 수정
- `android/app/build.gradle` — versionCode 42 → 43

---

## 세션: v43 실기기 피드백 7건 수정 (versionCode 44)

### 이슈별 수정 내역

| # | 현상 | 원인 분석 | 수정 |
|---|---|---|---|
| 1 | 화면 깨짐(즐겨찾기+오늘한일이 너무 큼) | `bottomGroup`(즐겨찾기+로그+버튼)이 항목 수에 따라 flex 공간을 무한 점유, micWrap이 찌그러짐 | `maxHeight: Math.round(screenHeight * 0.36)` 추가 + 로그 표시 최대 2개로 제한 |
| 2 | 로딩 메시지 안 바뀜 | `setInterval` cycler가 2200ms마다 실행 — Gemini 응답이 1~2초이면 interval이 한 번도 안 울리고 화면이 전환됨 | interval을 800ms로 단축, 초기 인덱스 `i = -1`로 설정해 800ms에 첫 메시지 변경 |
| 3 | 음성 인식 중 마이크가 파란색 | `LargeMicrophoneButton.tsx` listening 색상이 `"#1D3DBF"` | `Colors.danger`(빨간색)로 변경 |
| 4 | "엔화 얼마야" → 달러 검색 | Naver 링크에 항상 `query=환율`로 고정 | `currencyMap: [RegExp, string][]` 배열로 음성에서 통화 추출 — 엔화/위안/유로/파운드/달러 각각 다른 검색어 |
| 5 | 관계 선택 화면 검색 없음 | v43에서 검색 제거 | TextInput 검색창 복원, `relSearch` state 재추가. 단, 선택 즉시 저장(`saveMapping`)하고 Alert/즐겨찾기 자동 추가 없음 |
| 6 | 즐겨찾기 2번째~ 항목 "전화" 버튼 안 보임 | `callBtn`에 명시적 width 없어 긴 이름 항목에서 버튼이 0으로 압축 | `width: 72` 추가 |
| 7 | 음성 답변 화면 단조로움 | `general_answer`가 단순 텍스트 카드 | Gemini 스타일 채팅 버블 레이아웃: 사용자 질문(우측 파란 버블) + AI 답변(좌측 흰 버블 + ✨ 아바타) |

### 막혔던 지점: 로딩 메시지 cycler 타이밍

v43에서 cycler를 추가했지만 실제로 메시지가 변하지 않았다. 원인:

- Gemini API 응답은 보통 1~2초
- `setInterval(callback, 2200)` → 2.2초마다 실행
- 응답이 1.5초에 오면 interval이 한 번도 실행되기 전에 `screen.type`이 `general_answer`로 바뀜 → cycler 클린업

**해결:** interval을 800ms로 줄이고, 초기 인덱스를 `i = -1`로 설정. 800ms 후 `i`가 0이 되어 첫 번째 대체 메시지 표시. 1초 응답이 와도 최소 한 번은 메시지가 바뀐다.

**교훈:** 메시지 cycler는 API 응답 시간보다 짧아야 한다. "몇 초 기다려야 보이는" UX는 "안 보이는 것"과 같다.

### 채팅 버블 레이아웃 설계

Gemini 앱의 채팅 UI를 참고해 구조를 잡았다:

- **사용자 버블(우측):** `borderBottomRightRadius: 4`(말풍선 꼬리 효과), 파란 배경, 마이크 배지
- **AI 버블(좌측):** `borderTopLeftRadius: 4`, 흰 배경 + 테두리, ✨ 아바타 원형
- 두 버블 아래 "🔊 다시 읽기" + "홈으로 돌아가기" 버튼

텍스트에서 마크다운(**, #, \n 등)을 제거하는 `stripMarkdown` 함수를 사용해 Gemini 응답을 깔끔하게 표시.

### 검증 결과

- `npx tsc --noEmit` — 에러 0개
- versionCode 44 APK 빌드 성공 (47s)

### 미검증 항목

- 실기기에서 `bottomGroup maxHeight 0.36`이 화면 깨짐을 완전히 막는지 (기기마다 화면 비율 다름)
- 로딩 메시지가 빠른 응답(0.8초 미만) 케이스에서도 제대로 동작하는지

### 변경 파일

- `app/(tabs)/index.tsx` — bottomGroup maxHeight, 로딩 cycler 800ms, 통화 추출 currencyMap, relSearch 복원, 채팅 버블 JSX + 스타일
- `src/components/LargeMicrophoneButton.tsx` — listening 색상 → `Colors.danger`
- `app/(tabs)/favorites.tsx` — callBtn `width: 72`
- `android/app/build.gradle` — versionCode 43 → 44

---

## 세션: v44 실기기 피드백 4건 수정 (versionCode 45)

### 이슈별 수정 내역

| # | 현상 | 원인 | 수정 |
|---|---|---|---|
| 1 | 홈 화면 "오늘 한 일" 하단 잘림 + 스크롤 없음 | idle 화면이 `flex:1` View 구조 → 하단 항목이 화면 밖으로 넘치면 클립 | idle 화면을 `ScrollView`로 교체, `contentContainerStyle.minHeight: screenHeight`로 짧은 콘텐츠에서도 꽉 채움. `micWrap`을 `flex:1` → `height: screenHeight*0.4` 고정 |
| 2 | 즐겨찾기 전화 버튼 너무 크다 | paddingHorizontal:16, fontSize: FontSize.caption | paddingHorizontal→10, width→64, fontSize→12로 축소 |
| 3+4 | 할아버지/할머니 등 관계명 연락처 못찾음 + 전화번호부 전체 노출 필요 | "누구에게 전화할까요" 화면이 AI 추천 후보만 표시 | 해당 화면 진입 시 전체 연락처 로드(`contactPickerAll`), 화면에 검색 TextInput + 전체 목록 표시 |
| 즐찾 | 즐겨찾기 4명 이상 추가 시 홈 탭에 반영 안됨 | `loadFavorites()`가 앱 시작 시 1회만 실행, 탭 전환 후 갱신 없음 | `useFocusEffect` 추가 — 홈 탭 진입할 때마다 `getFavorites()` 재호출 |

### 막혔던 지점: ScrollView 안 flex:1

`flex:1`은 부모 View의 남은 공간을 차지하는 개념이다. `ScrollView`의 `contentContainerStyle` 안에서는 `flex:1`이 동작하지 않는다 — ScrollView는 content를 화면 크기와 독립적으로 렌더하기 때문.

해결: `minHeight: screenHeight`를 `contentContainerStyle`에 추가. 콘텐츠가 적을 때는 최소 전체 화면, 많을 때는 그 이상으로 자연스럽게 확장.

`micWrap`도 같은 이유로 `flex:1` → 고정 `height: Math.round(screenHeight * 0.40)` 교체.

### 검증 결과

- `npx tsc --noEmit` — 에러 0개
- versionCode 45 APK 빌드 성공 (23s)

### 변경 파일

- `app/(tabs)/index.tsx` — ScrollView 전환 + contactPickerAll + useFocusEffect + phonebookLabel 스타일
- `app/(tabs)/favorites.tsx` — callBtn 크기 축소
- `android/app/build.gradle` — versionCode 44 → 45
