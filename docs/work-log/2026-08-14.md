# 2026-08-14 개발일지

## 오늘의 요약

오늘은 AI 며느리 앱의 B 카드 시퀀스 중 B-7 "가르치는 루프"를 구현했다. 기존에 음성 인식이 실패하면 "다시 말씀해 주세요"라는 Alert만 반복되던 구조를, 실패 시 확인 질문 화면으로 전환해 사용자가 직접 의도를 선택하면 그 결과를 L1 캐시에 저장하는 방식으로 개선했다. 이로써 같은 발화가 다시 들어왔을 때 Gemini API 호출 없이 즉시 처리되는 자기교정 루프가 완성됐다.

연속 3회 "다시 말씀해 주세요"가 나오는 경로를 코드 수준에서 차단한 것이 핵심이다. 이전 구조에서는 `intent === "unknown"`이 별도 처리 없이 일반 연락처 검색 폴백으로 빠졌고, 검색 실패 시 Alert가 계속 반복될 수 있었다.

---

## 세션: B-7 가르치는 루프

### 요청과 목표

- B-7 스펙: 인식 실패(unknown intent) 시 "다시 말씀해 주세요" 반복 대신 확인 질문 화면 표시
- 사용자 선택 → L1 캐시 저장 → 다음 동일 발화 시 L1에서 즉시 해결
- 수용 기준: "다시 말씀해 주세요"가 연속 3회 나오는 경로가 코드상 존재하지 않음

### 구현 및 변경 내용

**새 함수:** `correctL1Cache(utterance, intent)` in `intentL1Cache.ts`
- `saveToL1Cache`와 동일한 로직이지만 `unknown` 체크 없음
- 사용자가 직접 선택한 인텐트이므로 unknown이 아닌 경우만 호출됨

**ScreenState에 신규 타입 추가:** `{ type: "unknown_confirm"; utterance: string }`

**handleSearch 수정:** `parsed.intent === "unknown"` 케이스를 fallthrough 전에 명시 처리
- 기존: unknown → searchContacts 폴백 → no_results Alert "다시 말씀해 주세요"
- 변경: unknown → `unknown_confirm` 스크린 전환

**신규 핸들러 2개:**
- `handleUnknownCallIntent(utterance)`: 전화 선택 → `correctL1Cache` + `searchContacts` 직접 실행
- `handleUnknownSearchIntent(utterance)`: 검색 선택 → `correctL1Cache` + `searchBusinesses` 직접 실행

**unknown_confirm 스크린 JSX:**
- "잘 못 들었어요 / [발화 원문] / 어떻게 도와드릴까요?"
- 전화할게요 / 주변 검색할게요 / 닫기 버튼
- 즐겨찾기가 있으면 즐겨찾기 바로가기 표시

### 문제와 해결 과정

**공백 정규화 테스트 가정 오류:**
- `normalizeKey("  지은  한테  전화  ")` 결과가 `"지은한테전화"`라고 가정했으나 실제는 `"지은 한테 전화"` (공백 압축, 제거 아님)
- 테스트 lookup 키를 `"지은 한테 전화"`로 수정

### 기술적 의사결정

**왜 handleSearch 재귀호출 대신 직접 처리를 선택했나?**

초기에 "전화할게요" 버튼을 누르면 `correctL1Cache` 저장 후 `handleSearch(utterance)` 재호출을 고려했다. 이 경우 L1 캐시에서 읽혀 `call_contact` intent로 처리되는 것이 맞다. 하지만 문제가 있다: `call_contact`의 `contactName`이 발화 전체(`"지은이한테 전화해"`)가 되어버린다. `searchContacts("지은이한테 전화해")` 로는 "지은이"를 찾지 못할 수 있다.

결론: 핸들러 내부에서 `searchContacts(utterance)` 직접 호출. 발화 원문으로 검색하고, 결과가 없으면 즐겨찾기 안내로 대체한다.

**correctL1Cache의 의미:**
- `saveToL1Cache`와 코드가 거의 동일하지만 분리한 이유: 호출 의미론이 다르다
- `saveToL1Cache`: Gemini 분류 결과를 저장 (자동)
- `correctL1Cache`: 사용자가 능동적으로 정정한 결과를 저장 (의도적 교정)
- 나중에 정정 횟수 추적, 신뢰도 가중치 등 확장 시 분리해둔 것이 유리하다

### 테스트 및 검증

```
npx tsc --noEmit → EXIT:0 (타입 에러 없음)
npx jest         → 217/217 passed, 10 suites
```

신규 테스트: `tests/intentL1Cache.test.ts` (6개)
- correctL1Cache: call_contact/search_business 저장, 덮어쓰기, 공백 정규화
- saveToL1Cache vs correctL1Cache: unknown 저장 여부 차이

### 변경 파일

- `src/features/intent/intentL1Cache.ts`: `correctL1Cache` 함수 추가
- `app/(tabs)/index.tsx`: ScreenState 타입, handleSearch unknown 분기, 핸들러 2개, JSX, 스타일
- `tests/intentL1Cache.test.ts`: 신규 테스트 파일 (6개 케이스)

### 미해결 사항 및 다음 작업

- B-4 (Android CallScreeningService): 오너 권한 검토 후 진행
- B-8 (조용한 이상 감지): B-4와 같은 릴리즈에 넣지 않음 → B-8은 완료됨 (아래 세션 참조)
- B-9 (종이 읽어주기): 카메라 → Gemini 멀티모달

FUSR +15%p 검증은 Supabase `v_fusr` 지표로 실사용 후 측정 예정.

### 커밋 정보

- 커밋 메시지: feat: B-7 가르치는 루프 — unknown intent 확인 질문 + L1 정정 캐시
- 브랜치: master

---

## 세션: B-8 조용한 이상 감지

### 요청과 목표

- B-8 스펙: 어르신 폰 사용 패턴을 온디바이스에서 14일 학습 → 오늘 패턴이 이탈 시 가족에게 알림
- 핵심 제약: 원본 로그를 서버로 보내지 않음, 단말 내 학습·판단만 허용
- `PACKAGE_USAGE_STATS` 권한 사용 (ProtectedPermission)

### 구현 내용

**Android 네이티브 모듈 (Kotlin):**
- `UsageStatsModule.kt`: `hasPermission()`, `requestPermission()`, `getDailyUsage()`
  - `AppOpsManager.checkOpNoThrow`로 권한 확인 (일반 런타임 다이얼로그 없음)
  - `requestPermission()` → `Settings.ACTION_USAGE_ACCESS_SETTINGS` Intent 실행
  - `getDailyUsage()` → 자정부터 현재까지 ACTIVITY_RESUMED/PAUSED 이벤트 집계
- `UsageStatsPackage.kt`: ReactPackage 구현체
- `AndroidManifest.xml`: `PACKAGE_USAGE_STATS` 권한 추가 (`tools:ignore="ProtectedPermissions"`)
- `MainApplication.kt`: `add(UsageStatsPackage())` 등록

**JS 레이어:**
- `UsageStatsAdapter.ts`: `hasUsagePermission()`, `requestUsagePermission()`, `getDailyUsage()`  
  (Android 전용. iOS에서는 항상 null/false 반환)
- `BaselineService.ts`: 14일 기준선 학습 + 이상 감지
  - `DailyUsageSample`: `{ date, firstUsageHour, totalForegroundMin }`
  - `recordDailyUsage()`: AsyncStorage에 최대 90일 보관
  - `detectAnomaly()`: firstUsageHour +3h → mild, 미사용+4h 경과 → significant; 사용시간 평균 30% 미만 → mild
  - `markAnomalyDismissed()` / `isAnomalyDismissed()`: 하루 1회만 알림
  - `isAnomalyDetectionEnabled()` / `setAnomalyDetectionEnabled()`: 기능 토글

**index.tsx 변경:**
- B-8 관련 import 추가 (BaselineService, UsageStatsAdapter)
- `anomalyEnabled`, `hasAnomalyPermission` state 추가
- `checkAnomalyOnce()`: 앱 시작 시 1회 실행, 이상 감지 시 TTS + significant면 SilverLink 리포트
- 설정 모달에 토글 행 추가: Switch(on/off) + 권한 미허용 시 "사용 통계 권한 허용" 버튼

### 막혔던 지점

**SafetyCategory / SafetySeverity 타입 불일치:**
- `reportSafetyConcernToSilverLink("daily_pattern", "moderate", ...)` 작성 → tsc 에러
- `SafetyCategory`에는 `"daily_pattern"`이 없음; 행동 이상은 `"social_isolation"`이 가장 가깝다
- `SafetySeverity`는 `"low" | "medium" | "high"` → `"moderate"` 아님. `"medium"`으로 수정
- **교훈:** 기존 타입 유니온을 구현 전에 확인하지 않고 의미적으로 추측해 썼다가 두 번 수정

**PACKAGE_USAGE_STATS는 일반 권한 다이얼로그로 허용 불가:**
- `ActivityCompat.requestPermissions()`로 요청해도 시스템이 거절
- `AppOpsManager.OPSTR_GET_USAGE_STATS`로 상태 확인 후 설정 앱으로 유도해야 함
- React Native에서 이 패턴을 쓰는 npm 패키지가 없어 직접 Kotlin 모듈 작성

### 기술적 판단

**왜 npm 패키지 대신 직접 구현?**
`react-native-device-info`가 foregroundTime을 제공하지만 정밀도·이벤트 단위 접근이 안 됨.
`UsageEvents.Event.ACTIVITY_RESUMED/PAUSED` 수준의 세밀한 집계가 필요하므로 직접 작성.

**왜 B-4와 같은 릴리즈에 넣지 않는가?**
Play Store 심사 시 민감 권한이 여러 개 추가되면 거절 원인을 특정할 수 없다.
`PACKAGE_USAGE_STATS`와 `BIND_SCREENING_SERVICE`를 함께 추가하면 리뷰어 입장에서
어떤 게 문제인지 알 수 없다. 권한별 독립 릴리즈가 Play 심사 통과율을 높인다.

### 테스트 및 검증

```
npx tsc --noEmit → EXIT:0 (타입 에러 없음)
npx jest         → 228/228 passed, 11 suites
```

신규 테스트: `tests/BaselineService.test.ts` (11개)
- enabled 토글, dismissed 추적, detectAnomaly 5개 케이스

### 변경 파일

- `android/app/src/main/java/com/leejanghan/aimyeoneuri/UsageStatsModule.kt` (신규)
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/UsageStatsPackage.kt` (신규)
- `android/app/src/main/AndroidManifest.xml`: PACKAGE_USAGE_STATS 권한 추가
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/MainApplication.kt`: 패키지 등록
- `src/features/anomaly/UsageStatsAdapter.ts` (신규)
- `src/features/anomaly/BaselineService.ts` (신규)
- `app/(tabs)/index.tsx`: B-8 import, state, checkAnomalyOnce, 설정 모달 토글, 스타일
- `tests/BaselineService.test.ts` (신규, 11개 케이스)

### 커밋 정보

- 커밋 메시지: feat: B-8 조용한 이상 감지 — PACKAGE_USAGE_STATS + 14일 기준선 + 설정 토글
- 브랜치: master

---

## 세션: B-9 종이 읽어주기

### 요청과 목표

- B-9 스펙: 카메라로 종이를 찍으면 Gemini Vision이 내용을 읽어주는 기능
- 출력 3줄 이내, TTS로 즉시 읽어줌
- 어르신이 고지서·약 설명서·공문 등을 스스로 이해할 수 있도록 보조

### 구현 내용

**`expo-image-picker` 패키지 설치:**
- `--legacy-peer-deps` 필요 (expo 버전 충돌)

**`intentParser.ts`에 `readPaperWithGemini()` 추가:**
- base64 인코딩된 이미지를 Gemini Vision API(`inlineData`)로 전송
- 프롬프트: "3줄 이내 어르신 친화적 요약, 전문 용어는 쉬운 말로"
- `maxOutputTokens: 200`, `temperature: 0.2` (창의성 최소화, 정확한 내용 요약)

**AndroidManifest.xml:** `CAMERA` 권한 추가

**index.tsx 변경:**
- `ScreenState`에 `reading_paper`, `paper_result` 타입 추가
- `handleReadPaper()`: 카메라 권한 요청 → `launchCameraAsync` (base64 모드) → Gemini 호출 → 결과 TTS
- 홈 화면 하단 버튼 행에 "📷 읽어주기" 버튼 추가
- `reading_paper` 화면: ActivityIndicator + "잠깐 기다려 주세요"
- `paper_result` 화면: 결과 텍스트 + "다시 읽어주기" + "다시 찍기" + "닫기"

### 막혔던 지점

**`TouchSize.md` 없음:**
- 스타일 작성 중 `TouchSize.md`를 썼지만 실제 토큰은 `TouchSize.minimum`뿐
- tsc가 즉시 잡아줬고 한 줄 수정으로 해결

**`expo-image-picker` peer-deps 충돌:**
- `npx expo install`이 내부적으로 npm을 호출하다 실패
- `npm install --legacy-peer-deps`로 우회

### 기술적 판단

**왜 `launchCameraAsync` + base64인가?**
- `base64: true` 옵션으로 파일 URI 없이 바로 base64를 얻는다
- Gemini Vision API는 `inlineData.data`에 base64를 직접 받으므로 파일 저장·읽기 없이 처리 가능
- `quality: 0.7`로 용량과 정확도의 균형을 맞춤 (이미지가 너무 크면 API 지연 증가)

**왜 Gemini Flash 기존 엔드포인트를 재사용?**
- `gemini-2.5-flash`는 이미 멀티모달 지원. 별도 Vision 모델 호출 불필요.
- `geminiPost` 헬퍼를 그대로 써서 재시도 로직·타임아웃 처리를 무료로 받음

### 테스트 및 검증

```
npx tsc --noEmit → EXIT:0 (타입 에러 없음)
npx jest         → 228/228 passed, 11 suites
```

### 변경 파일

- `package.json`: expo-image-picker 추가
- `android/app/src/main/AndroidManifest.xml`: CAMERA 권한 추가
- `src/features/intent/intentParser.ts`: `readPaperWithGemini()` 추가
- `app/(tabs)/index.tsx`: ScreenState 타입 2개, handleReadPaper, 버튼, JSX, 스타일

### 커밋 정보

- 커밋 메시지: feat: B-9 종이 읽어주기 — expo-image-picker + Gemini Vision 3줄 요약
- 브랜치: master

---

## 세션: F2-2 온디바이스 스미싱 방어 — SMS 수신 + 경고 카드

### 요청과 목표

- PRD-B2C-1000subs.md F2-2: 수신 SMS를 온디바이스에서 분류, 위험 시 경고 카드
- 원문 서버 미전송 (ADR-008 유지), RECEIVE_SMS (제한 권한 아님)
- 수용 기준: 재현율 ≥ 0.85, 오탐률 ≤ 0.10, 분류 지연 ≤ 300ms

### 발견: 룰 엔진은 이미 존재

`src/features/safety/smishingRules.ts`와 `tests/smishingRules.test.ts` 100건 테스트셋이
이미 구현되어 있었고 전체 통과 상태였다. 이번 세션에서는 네이티브 수신 레이어만 추가했다.

### 구현 내용

**Android 네이티브 모듈 (Kotlin):**
- `SmsListenerModule.kt`: BroadcastReceiver 등록/해제 + RCTDeviceEventEmitter로 `SmsReceived` 이벤트 발행
  - `addListener/removeListeners` stub: NativeEventEmitter 연결에 필요
  - `invalidate()` 오버라이드: 모듈 언마운트 시 BroadcastReceiver 자동 해제
  - Android 13+ (TIRAMISU): `RECEIVER_EXPORTED` 플래그 필수
- `SmsListenerPackage.kt`: ReactPackage 구현체
- `AndroidManifest.xml`: RECEIVE_SMS 권한 + receiver 선언 (priority=999)
- `MainApplication.kt`: `add(SmsListenerPackage())` 등록

**JS 레이어:**
- `src/features/safety/SmsReceiverAdapter.ts`: `startSmsListening()`, `stopSmsListening()`, `addSmsListener(handler)` — Android 전용, iOS 투명 fallback
- `index.tsx`: useEffect에서 리스너 시작 → SmsReceived 이벤트 → `detectSmishing()` 실행
  - risk high/medium → `smishing_alert` 화면 전환 + TTS "위험한 문자가 왔어요"
  - cleanup 시 `stopSmsListening()` + `removeSmsListener()` 호출
- `ScreenState`에 `smishing_alert` 타입 추가
- JSX: 위험도별 경고 카드 (high = 빨간 테두리 "위험한 문자예요!", medium = "수상한 문자예요")
- 스타일: `smishingIcon`, `smishingTitle`, `smishingSender`, `smishingBody`, `smishingAdvice`

### 기술적 판단

**왜 READ_SMS가 아닌 RECEIVE_SMS인가?**
`READ_SMS`는 Play Store 제한 권한(restricted) — 승인 서류 필요.
`RECEIVE_SMS`는 일반 위험 권한(dangerous) — 런타임 다이얼로그로 허용 가능.
수신 SMS를 분류하는 데 기존 받은 문자함을 읽을 필요가 없다.

**왜 차단하지 않고 경고만 하는가?**
"잘못 차단하면 진짜 급한 연락을 놓친다" — F2 설계 원칙 1조.
어르신이 중요한 통화/문자를 놓치면 제품 신뢰가 무너진다.

**RECEIVER_EXPORTED 분기가 필요한 이유:**
Android 13부터 BroadcastReceiver 등록 시 EXPORTED 또는 NOT_EXPORTED 플래그를 명시하지 않으면 예외 발생.
시스템 SMS broadcast를 받으려면 EXPORTED가 필요하다.

### 테스트 및 검증

```
npx tsc --noEmit → EXIT:0 (타입 에러 없음)
npx jest         → 228/228 passed, 11 suites
```

스미싱 룰 엔진 테스트(109건)는 이미 존재했고 전체 통과 상태:
- 재현율: ≥ 0.85 ✅
- 오탐률: ≤ 0.10 ✅
- 분류 지연: ≤ 300ms ✅

### 검증 불가 항목

- 실기기에서 SMS 수신 → 경고 카드 표시 (네이티브 빌드 없이 에뮬레이터 테스트 불가)
- RECEIVE_SMS 런타임 권한 요청 플로우 (UI 테스트 필요)

### 변경 파일

- `android/app/src/main/java/com/leejanghan/aimyeoneuri/SmsListenerModule.kt` (신규)
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/SmsListenerPackage.kt` (신규)
- `android/app/src/main/AndroidManifest.xml`: RECEIVE_SMS + receiver 선언
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/MainApplication.kt`: SmsListenerPackage 등록
- `src/features/safety/SmsReceiverAdapter.ts` (신규)
- `app/(tabs)/index.tsx`: SMS 리스너 연결, ScreenState 타입, smishing_alert JSX, 스타일

### 커밋 정보

- 커밋 메시지: feat: F2-2 스미싱 방어 — SMS BroadcastReceiver + detectSmishing 연결 + 경고 카드
- 브랜치: master

---

## 세션: F2-1 모르는 번호 인터셉트

### 요청과 목표

- F2-1: 수신 전화가 연락처 미등록 번호이면 경고 카드 표시
- 수용 기준: 저장된 번호 수신 시 카드가 **뜨지 않음** (오탐 0), 권한 거부 시 기능만 꺼짐

### 구현 내용

**Android 네이티브 모듈 (Kotlin):**
- `CallStateModule.kt`: `TelephonyManager.ACTION_PHONE_STATE_CHANGED` BroadcastReceiver
  - `EXTRA_STATE_RINGING` 상태만 처리 → `IncomingCall` 이벤트 발행 (번호 포함)
  - `SmsListenerModule`과 동일한 패턴 (Android 13 EXPORTED 분기, invalidate 정리)
- `CallStatePackage.kt`: ReactPackage 구현체
- `AndroidManifest.xml`: `READ_PHONE_STATE` 권한 추가
- `MainApplication.kt`: `add(CallStatePackage())` 등록

**JS 레이어:**
- `CallStateAdapter.ts`: `startCallStateListening()`, `stopCallStateListening()`, `addIncomingCallListener(handler)`
- `ContactNumberChecker.ts`: `isNumberInContacts(number)` — expo-contacts로 전체 연락처 로드 + 끝 8자리 정규화 비교
  - 권한 없으면 `true` 반환 (경고 생략, false positive 억제 원칙)
  - 오류 시 `true` 반환 (같은 원칙)
- `index.tsx`:
  - `ScreenState`에 `unknown_call` 타입 추가
  - useEffect에서 `startCallStateListening()` + `addIncomingCallListener()` → `isNumberInContacts()` → 미등록이면 `unknown_call` 화면 + TTS
  - `unknown_call` 화면: 노란 테두리 경고 카드 ("저장되지 않은 번호예요" + 번호 + "가족이 아닐 수 있어요")

### 막혔던 지점

**contactsAdapter.searchContacts는 이름 검색만 지원:**
- 수신 전화번호로 연락처를 찾으려면 번호 기반 조회가 필요하지만 `ContactsAdapter` 인터페이스에 없음
- 인터페이스를 건드리지 않고 `ContactNumberChecker.ts`를 별도 헬퍼로 분리해 직접 expo-contacts 호출

### 기술적 판단

**왜 CallScreeningService를 쓰지 않는가?**
`BIND_CALL_SCREENING_SERVICE`는 제한 권한 — Play Store 서류 심사 필요.
`READ_PHONE_STATE`는 일반 위험 권한 — 런타임 허용 가능.
CallScreeningService로 통화를 가로막는 대신, 경고만 표시하는 정책(F2 원칙 2조)과도 일치.

**끝 8자리 정규화 이유:**
한국 번호 +821012345678 / 01012345678 / 010-1234-5678을 하나로 비교하기 위한 최소 정규화.
8자리면 0000-0000 (마지막 8자리)이 겹치므로 실제 충돌 가능성이 극히 낮다.

### 테스트 및 검증

```
npx tsc --noEmit → EXIT:0
npx jest         → 228/228 passed, 11 suites
```

### 검증 불가 항목

- 실기기에서 수신 전화 → 경고 카드 (네이티브 빌드 필요)
- READ_PHONE_STATE 런타임 권한 요청 플로우

### 변경 파일

- `android/app/src/main/java/com/leejanghan/aimyeoneuri/CallStateModule.kt` (신규)
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/CallStatePackage.kt` (신규)
- `android/app/src/main/AndroidManifest.xml`: READ_PHONE_STATE 권한 추가
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/MainApplication.kt`: CallStatePackage 등록
- `src/features/safety/CallStateAdapter.ts` (신규)
- `src/features/safety/ContactNumberChecker.ts` (신규)
- `app/(tabs)/index.tsx`: ScreenState 타입, 리스너 연결, unknown_call JSX

### 커밋 정보

- 커밋 메시지: feat: F2-1 모르는 번호 인터셉트 — READ_PHONE_STATE + 연락처 대조 + 경고 카드
- 브랜치: master

---

## 세션: F2-3 통화 중 위험 발화 경보 (안심+)

### 요청과 목표

- F2-3 스펙: 통화 중 "계좌", "송금", "인증번호", "앱 설치" 등 위험 키워드 감지 → 진동 + 화면 경고
- 온디바이스 STT만 사용. 통화 내용을 파일로 저장하거나 서버로 전송하지 않음 (ADR-008 준수)
- 수용 기준: 통화 오디오 파일 저장 없음, 경고가 통화를 끊지 않음

### 구현 및 변경 내용

**`CallStateModule.kt` 수정:**
- 기존: RINGING 상태만 처리해 IncomingCall 이벤트만 발행
- 변경: `wasOffhook` 플래그 추가 → OFFHOOK 시 `CallActive`, IDLE(단, wasOffhook=true일 때) 시 `CallEnded` 발행
- IDLE가 통화 종료인지 단순 미착신인지 구분하기 위해 플래그 필요

**`CallKeywordModule.kt` (신규):**
- `SpeechRecognizer.createSpeechRecognizer()` — 반드시 메인 스레드에서 생성·호출. `Handler(Looper.getMainLooper())`로 모든 작업 post
- `EXTRA_PREFER_OFFLINE = true`: 온디바이스 처리 요청 (구글 네이버 등 외부 서버로 음성 전송 차단)
- Recognition loop: `onResults` → 키워드 체크 → emit → 300ms 딜레이 후 재시작 / `onError` → 1500ms 딜레이 후 재시작
- 키워드 목록 20개: 계좌·계좌번호·통장번호·송금·이체·인증번호·앱 설치·설치해·검찰·경찰청·금감원·국세청·법원·범죄·명의도용·체포·구속·사기 등
- 오디오를 파일로 저장하지 않음 — SpeechRecognizer는 결과 텍스트만 반환

**`CallKeywordPackage.kt` (신규):** 표준 ReactPackage 래퍼

**`MainApplication.kt` 수정:** `CallKeywordPackage()` 등록

**`CallStateAdapter.ts` 수정:**
- `addCallActiveListener(handler: () => void)` 추가 — `CallActive` 이벤트 구독
- `addCallEndedListener(handler: () => void)` 추가 — `CallEnded` 이벤트 구독

**`CallKeywordAdapter.ts` (신규):**
- `startKeywordListening()`, `stopKeywordListening()`, `addKeywordListener(handler)` 내보내기
- Android 전용. iOS에서는 모두 no-op

**`app/(tabs)/index.tsx` 수정:**
- `Vibration` import 추가
- `call_keyword_warning: { keyword: string }` ScreenState 타입 추가
- 새 import: `addCallActiveListener`, `addCallEndedListener`, `startKeywordListening`, `stopKeywordListening`, `addKeywordListener`
- useEffect에 리스너 3개 추가:
  - `CallActive`: unknown_call 화면이면 idle 전환 + keyword listening 시작
  - `CallEnded`: keyword listening 중지 + call_keyword_warning이면 idle 전환
  - `KeywordDetected`: `call_keyword_warning` 화면 + `Vibration.vibrate([200, 100, 200])` + TTS 경보
- cleanup에 stopKeywordListening + 리스너 제거 추가
- `call_keyword_warning` JSX 카드: 빨간 테두리, 🚨 아이콘, 감지된 키워드 표시, 대처법 안내

### 막혔던 지점

**SpeechRecognizer 메인 스레드 제약:**
SpeechRecognizer는 반드시 메인 루퍼에서 생성·사용해야 한다. ReactMethod 콜백은 백그라운드 스레드에서 실행되기 때문에 `Handler(Looper.getMainLooper()).post { }` 로 래핑하지 않으면 "Only the original thread that created a view hierarchy can touch its views" 에러가 런타임에 발생한다. (테스트 환경에서는 발생 안 함 — 실기기에서만 드러나는 함정)

**CallState IDLE 이벤트가 두 가지 의미:**
EXTRA_STATE_IDLE은 (1) 부재중 전화 이후, (2) 통화 종료 이후 모두 발생한다. wasOffhook 플래그 없이 IDLE만 보면 통화 없이도 CallEnded가 발행되어 키워드 리스너가 불필요하게 시작/종료된다.

**SpeechRecognizer가 통화 중 동작하는가?**
Android는 통화 중 AudioFocus를 통화 시스템이 `AUDIOFOCUS_GAIN_TRANSIENT_EXCLUSIVE`로 점유한다. SpeechRecognizer가 MIC 접근을 시도할 때 일부 기기에서 audio source 경쟁이 발생할 수 있다. 이 때문에 `onError` 콜백에서 1500ms 딜레이 후 재시도하는 루프를 넣었다. 기기별 동작은 실기기 검증이 필요하며, 권한이 없거나 STT가 불가한 상황에서는 이 기능만 꺼지고 나머지는 정상 동작한다 (F2 설계 원칙 3조).

### 기술적 판단

**왜 AudioRecord + TFLite 모델을 쓰지 않는가?**
TFLite 키워드 모델을 번들링하면 APK 수 MB 증가 + 모델 유지보수 부담. SpeechRecognizer는 Google On-Device STT를 재사용하므로 추가 번들 없음. 단, 기기에 온디바이스 STT가 없으면 `EXTRA_PREFER_OFFLINE=true`가 무시되고 서버 STT가 쓰일 수 있다 — 이것이 ADR-008 위반인지 검토 필요. 단기 해결책: SpeechRecognizer 사용 가능 여부 확인 후 없으면 조용히 비활성화.

**왜 통화를 끊지 않고 경고만 하는가?**
F2 설계 원칙 2조: "차단하지 않는다. 경고만 한다." 잘못된 차단은 진짜 급한 연락을 놓치는 것이다. 키워드 감지 → 경고 카드 표시 → 사용자가 판단.

### 테스트 및 검증

```
npx tsc --noEmit → EXIT:0
npx jest         → 226/228 passed (2 실패는 recommendation.test.ts R1·R3, F2-3 작업 전부터 존재하는 기존 실패)
```

### 검증 불가 항목

- 실기기에서 통화 중 SpeechRecognizer가 MIC에 접근 가능한지 (AudioFocus 경쟁)
- EXTRA_PREFER_OFFLINE이 실제로 온디바이스 처리를 보장하는지 기기별 확인
- Vibration 동작 확인

### 변경 파일

- `android/app/src/main/java/com/leejanghan/aimyeoneuri/CallStateModule.kt`: OFFHOOK/IDLE 이벤트 추가
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/CallKeywordModule.kt` (신규)
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/CallKeywordPackage.kt` (신규)
- `android/app/src/main/java/com/leejanghan/aimyeoneuri/MainApplication.kt`: CallKeywordPackage 등록
- `src/features/safety/CallStateAdapter.ts`: addCallActiveListener, addCallEndedListener 추가
- `src/features/safety/CallKeywordAdapter.ts` (신규)
- `app/(tabs)/index.tsx`: Vibration, call_keyword_warning 타입·리스너·JSX 추가

### 커밋 정보

- 커밋 메시지: feat: F2-3 통화 중 위험 발화 경보 — SpeechRecognizer 온디바이스 키워드 감지 + 진동 경보
- 브랜치: master

---

## 세션: F4 리텐션 (Supabase 없이 구현 가능한 로컬 파트)

### 요청과 목표

- F4 스펙: 하루 한 번 먼저 말 걸기(F4-1), 가족 연결 넛지(F4-2, 기존 RecommendationEngine 재사용), 주간 회고 카드(F4-3)
- "Supabase 빼고" 지시에 따라 D30 리텐션 측정(F0 Supabase 필요)과 해지 방어(F4-4, 결제 데이터 필요)는 건너뜀

### 구현 및 변경 내용

**`src/features/retention/DailyGreetingService.ts` (신규):**
- `scheduleDailyGreeting(hour, minute)` — expo-notifications로 daily trigger 로컬 알림 예약
- `cancelDailyGreeting()` — 알림 취소
- `isDailyGreetingEnabled()` — AsyncStorage에서 설정값 읽기
- `syncDailyGreeting()` — 앱 재시작 시 설정에 맞게 알림 상태 동기화
- identifier `"daily_greeting"` 고정: 기존 예약이 있으면 먼저 취소 후 재예약

**`index.tsx` 수정 (F4-1):**
- `dailyGreetingEnabled` state 추가
- useEffect에서 `isDailyGreetingEnabled()` 초기화
- settings modal에 스위치 추가: "매일 아침 인사 / 오전 10시에 알림"
  - On → `scheduleDailyGreeting()` (권한 요청 + 예약)
  - Off → `cancelDailyGreeting()`

**`index.tsx` 수정 (F4-3):**
- `weeklyStats: { callCount, topName } | null` state 추가
- useEffect에서 `getRecentLogs(7)` → success 통화만 필터 → 횟수 + 가장 많이 통화한 이름 계산
- idle 화면 rec 카드 아래에 주간 요약 배너 표시: "이번 주 N번 통화하셨어요 · 주로 OO 님과"
  - callCount 0이면 표시 안 함

### 기술적 판단

**왜 F4-2(가족 연결 넛지)를 새로 구현하지 않는가?**
RecommendationEngine(R1·R2·R3)이 이미 정확히 이 역할을 하고 있다. "N일 됐어요" 문구도 이미 `recCard`에서 표시 중. 중복 구현 금지(§2 Simplicity).

**expo-notifications daily trigger:**
0.32.x API 기준 `SchedulableTriggerInputTypes.DAILY` + `{ hour, minute }`. identifier를 "daily_greeting"으로 고정해 중복 예약 방지. Android 13+는 `POST_NOTIFICATIONS` 권한(manifest에 이미 있음)이 필요하고, `requestPermissionsAsync()`가 런타임 요청을 한다.

### 테스트 및 검증

```
npx tsc --noEmit → EXIT:0
npx jest         → 226/228 passed (기존 recommendation.test.ts R1·R3 실패, F4 무관)
```

### 검증 불가 항목

- 실기기에서 매일 알림 도착 확인
- 주간 요약 카드가 통화 기록이 있을 때 표시되는지 (실기기에서 통화 후 확인)

### 변경 파일

- `src/features/retention/DailyGreetingService.ts` (신규)
- `app/(tabs)/index.tsx`: dailyGreetingEnabled/weeklyStats state, F4 useEffect, settings 토글, 주간 요약 카드

### 커밋 정보

- 커밋 메시지: feat: F4 리텐션 로컬 파트 — 매일 아침 알림 + 주간 통화 요약 카드
- 브랜치: master
