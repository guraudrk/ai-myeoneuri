# 2026-08-05 개발일지

## 오늘의 요약

이번 세션에서는 카카오 장소 검색의 구조적 문제를 두 가지 축에서 해결하고, 업체 검색 외의 일반 질문(환율·날씨·주가 등)에 답변하는 기능을 새로 추가했다.

카카오 검색은 GPS 좌표와 `radius` 파라미터를 함께 보낼 경우 현재 위치에서 5km 이내 결과만 반환한다. "오목교역 근처 치킨집"처럼 사용자 위치와 멀리 떨어진 지역을 지목하면 결과가 0건으로 나왔던 원인이 여기 있었다. 쿼리에 지역명(역·동·구 등)이 포함된 경우 GPS 없이 키워드 관련도 기준으로 검색하고, 포함되지 않은 경우엔 GPS만 보내고 `radius`는 보내지 않는 방식으로 수정했다. 거리 뱃지가 지역명 쿼리에서도 표시되도록 GPS는 두 경우 모두 전달한다.

일반 질문 기능은 처음에 Gemini 호출 한 번으로 인텐트 분류와 답변을 동시에 처리하도록 설계했다가, Gemini가 프롬프트 예시 답변을 그대로 복사하는 문제와 학습 데이터 컷오프로 인한 날짜·주가 오류가 연달아 발생했다. 결국 호출을 두 단계로 분리(1차: JSON 분류만, 2차: 질문 원문을 제약 없이 전달)하고, 2차 호출에 Google Search 그라운딩(`tools: [{google_search: {}}]`)을 추가해 실시간 정보도 정확하게 답변하도록 완성했다.

---

## 세션: Kakao 검색 거리 제한 해제 + 지역명 분기

### 요청과 목표

- "오목교역 근처 치킨집 찾아줘" → 결과 0건 문제 해결
- 거리 제한 자체를 없애 달라는 추가 요청

### 구현 및 변경 내용

`KakaoBusinessSearchAdapter.ts`에서 검색 파라미터 조합을 두 케이스로 분기했다.

| 조건 | 파라미터 |
|---|---|
| 쿼리에 지역명 포함 (역/동/구/시 등) | `x`, `y` 만 전송 (sort 없음 → Kakao 관련도 기준 정렬) |
| 지역명 없음 | `x`, `y`, `sort=distance` 전송 |
| 공통 | `radius` 미전송 → 거리 제한 없음 |

GPS는 두 케이스 모두 전송하므로 `doc.distance` 필드가 채워져 거리 뱃지가 항상 표시된다.

### 문제와 해결 과정

- **원인**: Kakao API는 `x`, `y`, `radius`를 함께 받으면 radius 내 결과만 필터링한다. 오목교역은 사용자 GPS 기준 5km 밖이었으므로 0건.
- **1차 시도**: 지역명 포함 쿼리에서 GPS 자체를 보내지 않음 → 결과는 나왔으나 거리 뱃지 없음
- **최종**: GPS는 항상 보내되 radius만 제거. 지역명 쿼리는 sort도 제거해 Kakao 관련도 기준 유지.

### 변경 파일

- `src/features/business/KakaoBusinessSearchAdapter.ts`: 파라미터 분기 로직 교체

### 커밋 정보

- `e196ae3` feat: Kakao radius removed + general Q&A via Gemini
- `f994161` fix: Gemini real answers + distance for all queries + versionCode 3

---

## 세션: 일반 질문 Q&A 기능 (환율·날씨·주가 등)

### 요청과 목표

- 음식점 검색 외에 환율, 날씨, 잡다한 상식 질문에도 답변하는 기능 추가
- 음성 원문을 Gemini에게 그대로 전달해 답변을 받아 화면에 표시

### 구현 및 변경 내용

**최종 아키텍처 (2단계 Gemini 호출):**

```
음성 원문
  ↓
1차 Gemini 호출 (JSON 분류 프롬프트)
  → { intent: "general_question", utterance: "..." }
  ↓
2차 Gemini 호출 (질문 원문 그대로 + Google Search 그라운딩)
  → Gemini 날것 텍스트 답변
  ↓
stripMarkdown() → 화면 표시
```

**주요 변경:**
- `ParsedIntent`에 `general_question: { utterance: string }` 타입 추가
- `askGemini(question)` 함수 신설: `tools: [{google_search: {}}]` 포함
- `app/index.tsx`: `general_answer` 화면 상태 및 카드 UI 추가
- `stripMarkdown()`: `**굵게**`, `## 제목`, `[링크](url)` 등 마크다운 기호 제거

### 문제와 해결 과정

**1차 설계 실패: 단일 Gemini 호출로 분류+답변 동시 처리**
- JSON 형식 제약 안에서 답변을 요청하니 프롬프트 예시 답변(`"1달러는 약 1,350원"`)을 그대로 복사 출력
- 원인: few-shot 예시의 구체적 숫자를 Gemini가 템플릿으로 학습해 그대로 사용

**2차 설계 실패: 예시 제거 후에도 학습 데이터 오류**
- "어제 코스피 종가" 질문에 Gemini가 "6월 10일 2,701.17"을 답변 (실제는 8월 5일, 수치도 다름)
- 원인: Gemini의 학습 데이터 컷오프 — 실시간 데이터 없음

**최종 해결: 2단계 분리 + Google Search 그라운딩**
- 분류 호출(1차)은 JSON만, 답변 호출(2차)은 프롬프트 없이 질문 원문 그대로
- `tools: [{"google_search": {}}]` 추가 → Gemini가 Google 검색 후 답변

**마크다운 문제:**
- Gemini가 `**2,701.17**` 형태로 답변 → React Native `<Text>`가 기호 그대로 표시
- `stripMarkdown()` 함수로 렌더링 전에 제거

### 기술적 의사결정

**단일 호출 vs 2단계 분리:**
- 단일 호출: API 비용 절감, 레이턴시 낮음 / JSON 형식 제약이 자연어 품질을 해침
- 2단계 분리 채택: 인텐트 분류는 구조화된 JSON, 답변은 제약 없는 자유 텍스트 → 각 Gemini 호출이 본연의 역할에 집중
- 비용: 일반 질문당 Gemini 호출 2회. 현 단계(테스트)에서는 허용 가능

**Google Search 그라운딩:**
- Gemini 2.5 Flash REST API 레벨에서 `tools: [{google_search: {}}]` 한 줄로 활성화
- 실시간 데이터가 필요 없는 질문(역사·번역·계산)에도 항상 켜두는 것이 단순하며, 검색 비용은 Gemini 측에서 필요할 때만 발생

### 테스트 및 검증

- `npx tsc --noEmit`: 에러 없음 (모든 커밋 전 확인)
- Android Gradle 빌드: versionCode 2→3→4→5, 모두 `BUILD SUCCESSFUL`
- 실기기 테스트 (사용자 직접): "어제 코스피 종가" → 정상 동작 확인

### 변경 파일

- `src/features/intent/intentParser.ts`: `general_question` 타입, `askGemini()` 추가, 분류 프롬프트에서 `answer` 필드 제거
- `app/index.tsx`: `general_answer` 화면 상태, `stripMarkdown()`, `askGemini` import
- `android/app/build.gradle`: versionCode 1→5

### 미해결 사항 및 다음 작업

- 일반 질문 응답 레이턴시가 2단계 Gemini 호출로 길어질 수 있음 → 사용자 피드백 필요
- 답변 화면에 출처(Google Search 결과 URL) 표시 여부 검토
- "오목교역 치킨집" 거리 뱃지 실기기 확인 필요 (GPS 항상 전송 후 첫 빌드)
- 전반적인 UI 폴리싱 및 추가 기능 목록은 다음 세션 협의

### 커밋 정보

- `3d24dc0` fix: two-stage Gemini — classify then ask raw, versionCode 4
- `aea6367` fix: Google Search grounding + markdown strip, versionCode 5
- 브랜치: master

---

## 세션: 안전 신호 감지 + 앱 실행 기능 + Claude Design 브리프

### 요청과 목표

- SilverLink의 안전 신호 감지(7개 카테고리) 포팅
- "유튜브 켜줘" 같은 음성 앱 실행 기능
- Claude Design을 통한 UI 대대적 개선을 위한 디자인 브리프 작성

### 구현 및 변경 내용

**안전 신호 감지 (`safety_concern` 인텐트):**
- 넘어짐(fall_risk), 복약 미실시(medication_concern), 식사 미실시(nutrition_concern), 우울·외로움(mental_health_concern), 보행 불편(mobility_concern), 고립(social_isolation), 응급(urgent_medical) 7가지 카테고리
- 감지 시 빨간 안전 카드 + 119 바로 전화 버튼 표시

**앱 실행 (`open_app` 인텐트):**
- `APP_LINKS` 테이블: 유튜브, 카카오톡, 카카오맵, 네이버, 네이버지도, 인스타그램, 쿠팡, 날씨, 카메라, 설정, 전화, 문자, 갤러리, 지도 14개
- Android deeplink (`intent:#Intent;package=...;end`) 우선 시도, 실패 시 fallback URL
- `Linking.canOpenURL()` 사전 확인 후 실행

**어르신 특화 Gemini 시스템 프롬프트:**
- 할머니·할아버지 말투, 짧은 문장, 쉬운 단어, 핵심 먼저, 숫자 단위 명시, 요점 정리 6개 규칙
- `system_instruction` 파라미터로 Gemini에 전달

**Claude Design 브리프 작성 (`docs/DESIGN_BRIEF_FOR_CLAUDE.md`):**
- 앱의 해자: "어르신을 위한 Gemini 종합 비서" — 음성 하나로 전화·검색·질문·앱실행·안전감지
- 현재 tokens.ts 값 전체 (색상·폰트·터치 크기·간격·둥근 모서리·그림자)
- 5개 화면 ASCII 레이아웃 + 현재 문제점
- Toss·카카오 스타일 목표, 구체적 요청 6가지
- RN StyleSheet 기준 납품 형식 명시

### 기술적 의사결정

**안전 감지를 인텐트 분류에 통합:**
- 별도 키워드 매칭 대신 Gemini 분류 프롬프트에 `safety_concern` 카테고리 추가
- 장점: 애매한 발화("요즘 좀 힘들어")도 Gemini가 컨텍스트로 판단 가능
- 단점: Gemini API 응답 시간 의존 — 긴급 상황에서 레이턴시 리스크

**앱 실행 deeplink 방식:**
- 패키지명 기반 Intent URL이 가장 범용적 (앱이 설치됐을 때 무조건 작동)
- Samsung One UI 갤러리 패키지명(`com.sec.android.gallery3d`)은 기기마다 다를 수 있음 → 향후 개선 필요

### 변경 파일

- `src/features/intent/intentParser.ts`: `SafetyCategory`, `open_app`/`safety_concern` 인텐트, `openAppByName()`, `APP_LINKS`, `ELDERLY_SYSTEM_PROMPT`, `askGemini()` 전면 재작성
- `app/index.tsx`: `safety_alert`/`general_answer` 화면 상태, SAFETY_META, 관련 UI 스타일
- `docs/DESIGN_BRIEF_FOR_CLAUDE.md`: 신규 생성 (Claude Design 협업용 브리프)

---

## 세션: KB 앱 후보 선택 UI + "찾을 수 없어요" 팝업 + 디자인 개선 + APP_LINKS 대폭 확장 (versionCode 10)

### 요청과 목표

실기기 테스트(versionCode 9) 후 사용자 피드백 5가지:
1. KB국민은행 검색 시 여러 앱(KB증권·KB페이·KB국민은행) 중 선택 불가
2. 크롬 등 일부 앱 not found (HTTP 503 오류)
3. "찾을 수 없어요" 메시지가 팝업이 아닌 전체 화면 전환 → 어르신 가독성 낮음
4. 디자인 미흡 (헤더 "index" 노출, 레이아웃 비율, 시트 핸들 없음)
5. 화면 전환 시 애니메이션 없음

### 구현 및 변경 내용

**openAppByName 3단계 구조로 재설계:**
```
1순위: Gemini가 추론한 packageName → canOpenURL → 열기
2순위: APP_LINKS 테이블 (55개) → deeplink → fallback Play Store
3순위: APP_FAMILIES 테이블 (6개 패밀리) → ambiguous 반환 → 후보 선택 UI
```

**APP_LINKS 55개로 확장:**
- 기존 14개에서 대폭 확장
- 금융·은행: 토스, 카카오뱅크, 카카오페이, 케이뱅크, 뱅크샐러드, 신한은행, 우리은행, 하나은행, 농협은행, 기업은행, SC제일은행, KB국민은행·KB페이·KB증권
- 금융·카드: 삼성카드, 현대카드, 롯데카드, 국민카드, 신한카드, 우리카드, 농협카드, 삼성페이
- 금융·증권: 삼성증권, 키움증권, 한국투자증권, 미래에셋
- 쇼핑: 쿠팡이츠, 배달의민족, 요기요, 마켓컬리, 11번가, G마켓, 옥션, 오늘의집, 번개장터, 당근마켓
- 교통: 티맵, 카카오택시, 카카오버스, 코레일, SRT
- 엔터: 넷플릭스, 웨이브, 티빙, 왓챠, 쿠팡플레이
- 공공·헬스: 건강보험, 정부24, 손택스, 삼성헬스
- 미설치 시 fallback으로 Play Store 자동 안내 (크롬 503 문제 해결)

**APP_FAMILIES 신설 (ambiguous 앱 후보 선택):**
- KB: KB국민은행·KB페이·KB증권
- 카카오: 카카오톡·카카오맵·카카오뱅크·카카오페이
- 네이버: 네이버·네이버지도·네이버페이
- 삼성: 삼성페이·삼성헬스·삼성 인터넷
- 구글: 크롬·구글맵·구글 포토
- 크롬: 크롬·삼성 인터넷 (크롬 미설치 삼성 폰 대응)

**"찾을 수 없어요" → Alert.alert 팝업:**
- 기존: `setScreen({ type: "result", isError: true })` → 전체 화면 전환
- 변경: `Alert.alert("앱을 찾을 수 없어요", ...)` → 네이티브 다이얼로그
- 장점: 어르신이 현재 화면에서 바로 확인 가능, 확인 버튼 하나로 복귀

**헤더 숨기기 (index 제거):**
- `<Stack.Screen options={{ headerShown: false }} />`
- hero paddingTop: 56 하드코딩 → `(StatusBar.currentHeight ?? 24) + 8` 동적 계산
- hero flex: 0.62 → 0.65, inputSheet flex: 0.38 → 0.35
- 헤더(~56pt) 제거로 히어로 영역이 충분해짐

**화면 전환 fade 애니메이션:**
- `Animated.Value(0)` → `Animated.timing(1, 220ms)` — screen.type 변경마다 실행
- 결과 영역 전체를 `<Animated.View style={{ opacity: fadeAnim }}>` 로 래핑

**시트 핸들 (sheetHandle):**
- 폭 40pt, 높이 4pt 회색 바 — 바텀 시트처럼 보이도록
- ScrollView 최상단에 렌더링

### 막혔던 지점

**Bash/PowerShell cwd 리셋 문제:**
- Claude Code의 Bash 도구가 매 호출마다 SilverLink 디렉토리로 cwd를 리셋함
- `cd ai-myeoneuri && npx tsc` 패턴 전혀 안 됨
- 해결: `npx tsc --noEmit --project "C:/dev/silverlink-ai/ai-myeoneuri/tsconfig.json"` 로 절대 경로 지정

**Animated.View와 contentContainerStyle gap 이중 적용:**
- ScrollView의 `contentContainerStyle={{ gap: Spacing.md }}`와 안에 넣은 `<Animated.View style={{ gap: Spacing.md }}>` 가 공존
- contentContainerStyle의 gap은 직접 자식(Animated.View)이 하나뿐이므로 실제로 무해함
- 실질적인 gap은 Animated.View 안의 자식들에 적용됨

### 기술적 의사결정

**3단계 openAppByName vs 단순 패밀리 패칭:**
- 선택: Gemini 패키지 → APP_LINKS → APP_FAMILIES 순서
- 이유: 이미 Gemini가 정확한 패키지명 반환 가능하면 바로 열기. APP_LINKS는 Gemini 실패 백업. APP_FAMILIES는 진짜 모호한 경우에만 후보 제시.
- "KB페이 켜줘" → Gemini가 `com.kbstar.kbpay` 반환 → step1에서 해결 → APP_FAMILIES 불필요
- "KB 켜줘" → Gemini `package_name: ""` → step2 miss → step3 KB 패밀리 → 후보 3개 표시

**Alert.alert vs 커스텀 Modal:**
- Alert.alert 채택: 네이티브 다이얼로그로 어르신에게 가장 익숙한 패턴
- 커스텀 Modal은 복잡도 대비 UX 차별점 없음

### 변경 파일

- `src/features/intent/intentParser.ts`: `AppCandidate` / `OpenAppResult` 타입 신설, `APP_LINKS` 55개, `APP_FAMILIES`, `openAppByName` 3단계 재설계
- `app/index.tsx`: `Stack` import, `Animated` / `StatusBar` import, `app_candidates` ScreenState, fadeAnim, hero 스타일 수정, sheetHandle, app_candidates UI, appCandidateRow 스타일
- `android/app/build.gradle`: versionCode 9 → 10

### 검증

- `npx tsc --noEmit --project ...`: 에러 없음
- `assembleDebug`: BUILD SUCCESSFUL (1m 17s, 258 tasks executed)
