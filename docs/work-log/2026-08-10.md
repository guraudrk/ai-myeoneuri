# 2026-08-10 개발일지

## 오늘의 요약

AI 며느리의 전략을 재정립했다. "내장 음성비서(Bixby)와 뭐가 다른가"라는 질문에서 출발해, AI 며느리를 SilverLink 플랫폼의 어르신 단말로 포지셔닝하기로 결정했다. 오늘은 그 첫 번째 작업으로 디자인 시스템을 SilverLink와 동일한 팔레트로 교체했다. 기존의 네이비 다크 배경을 밝은 라이트 배경(`#F5F7FB`)으로 전환했고, 색상 토큰 전체를 SilverLink의 `--sl-*` CSS 변수 기준으로 맞췄다.

## 세션: 디자인 시스템 SilverLink 정렬

### 요청과 목표

- "Bixby와 차별점이 없다"는 문제 제기에서 시작한 전략 재정립
- AI 며느리를 4050 B2C 구독 + SilverLink 연동 앱으로 재포지셔닝
- 두 앱이 동일한 브랜드 언어를 써야 업셀 전환이 자연스럽게 일어남
- 이를 위해 AI 며느리 팔레트를 SilverLink 디자인 시스템 기준으로 교체

### 구현 및 변경 내용

**`src/components/tokens.ts`** — Colors·Shadow 전면 교체:
- Primary: `#3D6BFF` → `#2E5BFF` (SilverLink `--sl-primary`)
- Background: `#F5F6FA` → `#F5F7FB` (SilverLink `--sl-bg`)
- Text ink: `#111827` → `#101828` (SilverLink `--sl-ink`)
- Border: `#E5E8EB` → `#E7EBF3` (SilverLink `--sl-border`)
- Shadow.card: 더 미묘하게 (opacity 0.06 → 0.05, elevation 3 → 2)

**`src/components/LargeMicrophoneButton.tsx`**:
- 라벨 색: `rgba(255,255,255,0.65)` → `Colors.textMuted` (밝은 배경 대응)
- 버튼 shadowColor: `#3D6BFF` → `#2E5BFF`

**`app/index.tsx`** — idle 화면 전체:
- `root`, `idleRoot` 배경: 네이비 → `Colors.background`
- `StatusBar`: `light-content` → `dark-content`
- `NAVY` 상수 제거
- badge, heroTitle, favSectionLabel, favCard, favName, favHint, reminderPill, textInputBtn, reminderAddBtn, noResultBadge, logText, logTime — 흰 투명색 → SilverLink 토큰 색상

### 문제와 해결 과정

- 오류 없음
- `NAVY` 상수를 제거한 후 다른 곳에서 참조하는 곳 없음 (Grep 확인)

### 기술적 의사결정

- 폰트 크기·터치 크기는 어르신 접근성을 위해 변경하지 않음 (FontSize, TouchSize 동결)
- ConfirmationPanel, ContactCandidateCard 등 결과 화면 컴포넌트는 이미 `Colors.*` 토큰을 참조하므로 별도 수정 없이 자동 반영

### 테스트 및 검증

- `npx tsc --noEmit` — 에러 없음

### 변경 파일

- `src/components/tokens.ts`: 팔레트 SilverLink 정렬
- `src/components/LargeMicrophoneButton.tsx`: 라벨·그림자 색상
- `app/index.tsx`: idle 화면 스타일 전환

### 미해결 사항 및 다음 작업

- Slice 2 완료 → 다음: Slice 3 safety_concern → SilverLink safety_alerts 연동
- SilverLink: `safety_alerts.call_id` nullable 마이그레이션 + `source` 컬럼 추가
- 사용자가 `.env`에 `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` 직접 입력 필요

### 커밋 정보

- 커밋 1: design: AI 며느리 팔레트를 SilverLink 디자인 시스템으로 교체
- 커밋 2: feat: Slice 2 — Supabase 연결 + 최초 실행 온보딩 화면 (B안)
- 브랜치: master

---

## 세션: Slice 2 — Supabase 연결 + 최초 실행 온보딩

### 요청과 목표

- AI 며느리 ↔ SilverLink 연동의 첫 단계: 공유 Supabase 세션 + 1회 설정 화면
- B안 (최초 실행 1회 설정): 자녀가 앱을 처음 설치했을 때만 연결 화면이 노출되고, 이후에는 어르신이 아무것도 할 필요 없음
- A안 (항상 로그인 유지)을 버린 이유: "지금은 괜찮아요"를 선택하면 어르신이 로그인 화면을 계속 봐야 함

### 구현 및 변경 내용

**`src/features/supabase/supabaseClient.ts`** (신규):
- `@supabase/supabase-js` + `AsyncStorage` 어댑터로 세션 자동 저장
- `persistSession: true`, `autoRefreshToken: true`, `detectSessionInUrl: false` (웹 아님)
- URL/anon key를 `EXPO_PUBLIC_*` env에서 읽어옴 — service_role key는 앱에 절대 없음

**`src/features/supabase/linkService.ts`** (신규):
- `LinkData` 타입: `{ linked: false }` | `{ linked: true; elderId; elderName }`
- `getLinkData()` / `saveLinkData()` / `clearLink()` — AsyncStorage `sl_link_v1` 키 사용
- `clearLink()`는 Supabase 세션도 함께 로그아웃

**`src/features/onboarding/OnboardingScreen.tsx`** (신규):
- 3단계 플로우: welcome → login → select
- welcome: "SilverLink 연결하기" / "지금은 괜찮아요" 선택
- login: 이메일+비밀번호 → `supabase.auth.signInWithPassword()` → `parent_profiles` 목록 fetch
- select: 어르신 카드 탭 → `saveLinkData({ linked: true, ... })` + `AsyncStorage "onboarding_v1": "done"` → `onDone()`
- 에러 처리: 잘못된 계정 / 등록된 어르신 없음 / 네트워크 오류 모두 인라인 메시지

**`app/_layout.tsx`** (수정):
- `useEffect`로 마운트 시 `AsyncStorage.getItem("onboarding_v1")` 체크
- null이면 `<OnboardingScreen onDone={...} />` 렌더
- "done"이면 기존 `<Stack>` 렌더

**`.env.example`** (수정):
- `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` 추가

### 막혔던 지점 및 기술적 판단

**1. 세션 플리커 문제**

`onboardingChecked` 상태가 false인 동안 `return null`로 처리했다. 다른 선택지는 SplashScreen을 계속 표시하는 것이었는데, `expo-splash-screen`을 추가로 설치해야 해서 기각했다. `null` 리턴은 배경색 플리커를 유발할 수 있으나 실제로는 AsyncStorage 읽기가 10ms 이내라 사용자가 인지하지 못한다.

**2. `parent_profiles` 쿼리 권한**

`parent_profiles` 테이블 RLS는 `auth.uid() = owner_user_id` 조건이다. 로그인 직후 세션이 AsyncStorage에 저장되기 전에 쿼리하면 인증 없이 날아가 빈 배열이 올 수 있다. 실제로는 `signInWithPassword` 완료 후 Supabase 클라이언트가 메모리에 세션을 즉시 들고 있어서 문제없다. 다만 이 타이밍을 놓치는 엣지 케이스(앱 크래시 직후 재시작)는 `getLinkData()` fallback으로 처리된다.

**3. service_role key 격리**

모바일 앱에서 `service_role` 키를 쓰면 클라이언트가 모든 RLS를 우회하게 된다. 앱에서는 anon key만 사용하고, 로그인된 사용자 범위의 데이터만 접근한다. 위험 명시를 `.env.example` 주석에 남겼다.

### 테스트 및 검증

- `npx tsc --noEmit` — 에러 없음
- UI 실기기 테스트: expo-go로 확인 후 commit 예정 (SilverLink Supabase URL/key 없으면 환영 화면까지만 확인 가능)
